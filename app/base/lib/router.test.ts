// import { Base, IBaseComponent } from '../components/base'
// import router from './router'
// import { describe, expect, test } from '@jest/globals'

// test('Router initializes correctly', () => {
//   const view = Base()
//   const routes = {
//     '/': jest.fn(),
//   }
//   router.init({ routes, view })
//   expect(routes['/']).toHaveBeenCalled()
//   // Assert that the router has initialized the routes and view correctly.
//   // You might need to expose some internal state of the router for this test.
// })